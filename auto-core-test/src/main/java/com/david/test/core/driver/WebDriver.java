package com.david.test.core.driver;

import org.openqa.selenium.Capabilities;

public class WebDriver extends Driver {

    public WebDriver(Capabilities desiredCapabilities) {
        super(desiredCapabilities);
    }
}
